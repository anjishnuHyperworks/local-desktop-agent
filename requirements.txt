# UI
PyQt6>=6.6.0

# Screen capture
mss>=9.0.1

# Image processing
Pillow>=10.3.0

# Input emulation (physical coordinate input, global hotkeys)
pynput>=1.7.7

# PyAutoGUI — used only for FAILSAFE compatibility cue, not primary input
pyautogui>=0.9.54

# HTTP client for Grok Vision API
httpx>=0.27.0

# Clipboard safety
pyperclip>=1.8.2

# Database (stdlib sqlite3 used; no extra package needed)

# Environment variable loading
python-dotenv>=1.0.1
